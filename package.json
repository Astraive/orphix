{
  "name": "orphix",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @orphix/desktop dev",
    "build": "pnpm --filter @orphix/desktop build",
    "build:core": "cargo build -p orphix-core",
    "dev:core": "cargo build -p orphix-core",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.0.0"
}
