// Legacy theme type — prefer @orphix/themes directly
export interface OrphixTheme {
  name: string;
  bg: string;
  surface: string;
  muted: string;
  accent: string;
  text: string;
  textDim: string;
  border: string;
  borderActive: string;
  panelBg: string;
  terminalBg: string;
  terminalFg: string;
  terminalCursor: string;
  terminalSelection: string;
}
