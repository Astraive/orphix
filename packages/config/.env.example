# Config Package Environment Variables
# These are read by packages/config/src/trust.ts (lazy getters).
# Each service loads its own .env before these are accessed.

# Service URLs (comma-separated or single values)
CONTROL_API_URL=http://localhost:2605
LINK_API_URL=http://localhost:2606
MARKETPLACE_API_URL=http://localhost:2607
